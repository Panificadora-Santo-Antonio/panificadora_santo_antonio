<%= form_with(model: product_sale, local: true) do |form| %>
  <% if product_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_sale.errors.count, "error") %> prohibited this product_sale from being saved:</h2>

      <ul>
        <% product_sale.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :totalProductPrice %>
    <%= form.text_field :totalProductPrice %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
