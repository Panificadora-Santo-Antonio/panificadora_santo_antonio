<%= form_with(model: [@sale, @product_sale], local: true) do |form| %>
  <% if product_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_sale.errors.count, "error") %> prohibited this product_sale from being saved:</h2>

      <ul>
        <% product_sale.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
    <%= form.label :produto_id %>
    <%= form.select :produto_id, @products, {include: true}%>
  </div>


  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantity, id:"quantity" %>
  </div>

  


  

  <div class="actions">
    <%= form.submit id:"submit" ,:value =>"Confirmar" %>
  </div>
<% end %>
