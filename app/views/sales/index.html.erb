<p id="notice"><%= notice %></p>

<h1>Vendas</h1>

<table>
  <thead>
    <tr>
      <th>Cliente |</th>
      <th>Vendedor |</th>
      <th>Valor Total |</th>
      <th>Data</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <td><%= sale.customer.name %></td>
        <td><%= sale.user.name %></td>
        <td><%= sale.totalValue %></td>
        <td><%= sale.date_time %></td>
        <td><%= link_to 'Mostrar', sale %></td>
        <td><%= link_to 'Apagar', sale, method: :delete, data: { confirm: 'Você tem Certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Venda', new_sale_path %>
<%= link_to 'Voltar', root_path %>
